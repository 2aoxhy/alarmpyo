# 알람표 V10 디자인 QA

검증일: 2026-08-15

대상: `V10 · 1.0.10(10)` 공통 React Native 화면, Android 권한 이동, 4×1 위젯과 Play 등록 자산 계약

## 반영한 개선

- 오늘 화면의 알람 상태를 제목과 설명으로 분리하고 권한·오류·조치 문구의 말줄임 제한을 제거했습니다.
- 설정의 근무표 요약은 일반 화면에서 `주 06:45 · 야 17:45`처럼 근무명과 시간을 묶고, 320dp 또는 글자 130% 이상에서는 `근무 시간 설정됨`으로 줄입니다.
- 좁은 화면이나 큰 글자에서 `ListRow` 설명이 자연스럽게 재배치되며, 하단 탭 안전 여백이 화면별 여백보다 우선하도록 고정했습니다.
- 정확 알람, 앱·채널 알림, 수면 알림, 전체 화면, 방해 금지, 배터리 최적화와 앱 정보에 구조화된 Android 설정 이동 계약을 적용했습니다.
- 위젯 카드와 보조 패널을 중립 다크그레이로 통일하고 근무 의미색은 작은 아이콘에만 남겼습니다.
- 고정 날짜·시간이 보이던 구형 위젯 선택기 미리보기를 중립 `previewLayout`·`previewImage`로 교체하고, Android 15+에는 실제 저장 스냅샷 기반 생성형 미리보기를 제한적으로 제공합니다.
- V10 Play 스크린샷 순서와 대체 텍스트를 manifest로 고정하고, 실기기 WebP를 무왜곡 1080×1920 RGB PNG로 준비하는 도구를 추가했습니다.

## 자동 검증 결과

- 고정 도구 버전: Node.js 24.16.0, npm 11.13.0 통과
- TypeScript, 전체 ESLint, Vitest 153파일·1,143건 통과
- Android 네이티브 JDK 17·SDK 36 검사: 77개 작업 `BUILD SUCCESSFUL`
- Expo Doctor: 20/20 통과
- 의존성·도구 감사: 신규 또는 만료된 high·critical 없음
- 브랜드 파생 자산 7개와 마스터 일치
- Play 구성, 공개 개인정보처리방침, 스크린샷 누락 허용 자산 검사 통과
- 엄격 Play 자산 검사는 의도대로 V10 실기기 스크린샷 0장을 차단

## 실기기에서 남은 출시 검증

- 현재 ADB가 연결 기기를 표시하지 않습니다. USB 디버깅 승인과 Windows 드라이버 연결을 먼저 해결합니다.
- Samsung Android에서 320·360·412dp와 100·130·150·200% 글자 크기, TalkBack 낭독 순서를 확인합니다.
- API 31~36에서 각 권한 행이 의도한 설정 또는 안전한 대체 화면을 여는지 확인합니다.
- V08(8) Alpha에서 V10(10)으로 Play 업데이트해 근무표, 메모, 권한, 알람, 타이머와 기존 위젯이 보존되는지 확인합니다. V09는 사용하지 않습니다.
- One UI 선택기와 기존 4×1 위젯에서 주간·야간·휴무·교육·예비군, 사용자 시간 변경과 200% 글자를 확인합니다.
- 실제 V10 Play 설치본으로 `01-brand`, `02-today`, `03-timer`, `04-settings`를 다시 촬영하고 엄격 자산 검사를 통과합니다.

현재 결과: 자동 검증 통과, 실기기·Play 스크린샷 검증 대기
