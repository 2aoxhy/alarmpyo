# 알람표 V07 디자인 QA

검증일: 2026-08-15

대상: 로컬 Expo Web 미리보기와 Android용 공통 React Native 화면
기준 화면: 사용자가 제공한 V05 실기기 캡처 9장과 `.release/product-audit-20260815/` 기준 캡처

## 검증 범위

- 오늘, 달력, 달력 표시 안내, 설정, 첫 설정 3단계, 근무표 설정을 동일한 모바일 뷰포트에서 비교했다.
- 360×800에서 기존 화면과 V07 화면을 같은 상태로 나란히 비교했다.
- 320×800에서 오늘과 설정을 별도로 확인했다.
- 키보드로 달력 표시 안내 시트를 열고 닫아 초기 포커스, Tab/Shift+Tab 순환, Escape 닫기, 호출 버튼으로 포커스 복귀를 확인했다.
- 주요 버튼, 탭, 설정 행, 접기/펼치기 영역이 실제로 동작하는지 확인했다.

## 반영한 개선

- 첫 설정의 7개 근무 방식 카드를 `주간 고정 / 2교대 / 3교대 / 기타` 4개 분류로 줄였다.
- 순서와 시간은 동시에 모두 펼치지 않고 요약을 먼저 보여준 뒤 필요한 편집기 하나만 연다.
- 첫 근무일 화면의 설명과 선택 계층을 단순화했다.
- 설정 첫 화면은 4개 메뉴를 유지하고 설명을 짧게 정리했다.
- 근무표 설정은 `근무 방식 / 근무 시간 / 출근 루틴` 3개 요약 허브로 정리했다.
- 달력의 긴 범례를 본문에서 제거하고 접근 가능한 시트로 옮겼다.
- 날짜 편집과 알람 설정은 핵심 상태를 먼저 보여주고 세부 항목을 단계적으로 펼치게 했다.
- 320dp 오늘 화면의 남은 시간과 일정 수정 버튼을 세로로 재배치해 한 글자 고아 줄바꿈을 제거했다.
- 웹 시트에서 발생하던 `findNodeHandle is not supported on web` 오류를 직접 DOM 포커스 처리로 수정했다.

## 결과

- 360×800: 비교한 핵심 화면에서 잘림, 겹침, 의도하지 않은 가로 스크롤이 없었다.
- 320×800: 오늘 하단 정보와 설정 메뉴가 잘리지 않았고 하단 탭의 48dp 조작 영역이 유지됐다.
- 달력 표시 안내: 긴 내용은 시트 내부에서 세로 스크롤되며 배경 화면을 가리지 않는다.
- 첫 설정: 첫 화면의 선택 수가 4개로 줄었고 주요 선택에서 다음 단계까지 2번 이내 조작으로 이동한다.
- 텍스트 계층과 경계는 기존 다크그레이 디자인 토큰을 유지한다.
- 전체 JavaScript 검사 1,123건, 타입 검사, 린트, 의존성 감사, Expo Doctor 20개 항목, Android 네이티브 77개 작업이 통과했다.

## 실기기에서 남은 출시 검증

- Samsung Android에서 100/130/150/200% 글자 크기와 TalkBack 낭독 순서를 확인한다.
- V05(6)에서 V07(7)로 업데이트해 근무표, 메모, 권한, 알람과 타이머가 보존되는지 확인한다.
- 잠금, 앱 종료, 재부팅, 시간 변경, 시간대 변경에서 근무 알람·수면 알림·타이머와 5분 재알림을 검증한다.
- 실제 V07 Play 설치본으로 스토어 스크린샷을 새로 촬영한다.

final result: passed
