# 알람표 V13 디자인 QA

검증일: 2026-08-20

대상: 달력 공휴일·급여일·근무 표시 분리, 날짜 상세 정보, 달력 표시 안내

## 검증 근거

- 문제 화면: `Screenshot_20260820_203443.webp` (628×1280)
- 구현 화면: `implementation-calendar-320x800-final.png` (320×800 CSS px)
- 범례 화면: `implementation-legend-320x800-final.png` (320×800 CSS px)
- 날짜 상세: `implementation-day-detail-360x800.png` (360×800 CSS px)
- 전후 비교: `comparison-before-v13.png`

문제 화면은 기존 상태를 보여 주는 참고 자료이므로 픽셀 복제 대상이 아닙니다. 같은 달력 영역에서 날짜 의미와 근무 의미가 서로 다른 형태와 행으로 분리되는지를 중점적으로 비교했습니다.

## 반영한 개선

- 공휴일은 산호색 둥근 사각형 `공`, 급여일은 호박색 원형 `급` 또는 `급*` 표식으로 분리했습니다.
- 공휴일과 급여일이 겹치면 `공 → 급` 순서로 표시하며, 아래에는 근무 또는 특별 일정 배지 하나만 유지합니다.
- 대체공휴일의 `대체` 텍스트 배지를 제거하고 날짜 상세에서 전체 명칭을 제공합니다.
- 표식은 별도 TalkBack 초점을 만들지 않으며 날짜 셀 전체 설명에서 공휴일, 급여일, 근무, 메모, 직접 변경과 선택 상태를 한 번씩 전달합니다.
- 날짜 편집 화면 상단에 조건부 `날짜 정보` 카드를 추가하고 공휴일 전체 명칭과 급여일 조정·예상 상태를 표시합니다.
- 표시 안내에 `공`, `급`, `급*` 의미를 추가하고 320dp에서 긴 설명이 두 줄로 안전하게 줄바꿈되도록 했습니다.
- 급여일이 다음 급여월에서 이전 달로 당겨지는 월 경계와 지원 연도 상한을 달력·상세에서 동일하게 처리합니다.

## 시각 및 상호작용 QA

- 320dp 달력에서 `공`·`급` 표식과 근무 배지가 서로 다른 행에 표시되며 날짜 숫자, 선택 체크, 메모, 직접 변경선과 겹치지 않습니다.
- 320dp 표시 안내에서 긴 급여 설명은 부모 폭 262px 안에서 197×36px로 두 줄 배치되며 viewport를 넘지 않습니다.
- 공휴일과 급여일 표식은 글자와 형태가 달라 회색조에서도 구분할 수 있습니다.
- 날짜 선택과 표시 안내 열기·닫기, 날짜 상세 진입을 실제 웹 렌더에서 확인했습니다.
- 콘솔에서 이번 변경으로 새로 발생한 오류는 확인되지 않았습니다.

## 자동 검증

- 320·360·412dp 및 글자 100·140·200% 반응형·접근성 소스 계약
- 공휴일+휴무, 대체공휴일+야간, 급여일+주간, 공휴일+급여일+대체근무 조합
- 오늘·선택·메모·직접 변경이 함께 있는 최대 상태
- 공휴일→급여일 표식 순서와 예상 급여일 `급*`
- 월 경계 급여일과 2200년 12월 경계
- TalkBack 중복 방지, 셀 메모 비교와 안정 콜백

현재 결과: V13 구현 및 웹 시각 QA 통과. EAS 빌드와 Play 배포는 취소 상태를 유지합니다. Android 실기기의 글자 140·200%와 TalkBack 최종 확인은 향후 V13 빌드 재개 시 수행해야 합니다.
